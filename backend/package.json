{
  "name": "adw-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts",
    "generate:demo": "ts-node --transpile-only src/scripts/generateDemoData.ts",
    "import:local-dictionaries": "ts-node --transpile-only src/scripts/importLocalDictionaries.ts",
    "import:documents": "ts-node --transpile-only src/scripts/importDocumentsData.ts",
    "build": "tsc",
    "start": "node dist/app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.19",
    "@types/node": "^20.14.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4",
    "@types/pg": "^8.10.4"
  }
}
